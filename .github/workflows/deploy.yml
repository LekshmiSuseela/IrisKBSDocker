name: CI/CD Pipeline for IRIS API

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: iris-api
  GAR_LOCATION: us-central1-docker.pkg.dev/mlops-474118/iris-api-repo/iris-api

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Workflow started
        run: |
          echo "🚀 CI/CD triggered on branch ${{ github.ref }}"
          echo "🔧 Running on commit ${{ github.sha }}"
          echo "🔹 Image name: $IMAGE_NAME"
          echo "🔹 Artifact Registry path: $GAR_LOCATION"
